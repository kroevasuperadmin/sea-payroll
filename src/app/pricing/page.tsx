import { PlaceholderPage } from "@/components/ui";

export default function Pricing() {
  return (
    <PlaceholderPage
      eyebrow="Pricing"
      title="Free on devnet, always."
      body="Tiba is a devnet demo — no fees, no plans, no billing. A mainnet pricing model (a small basis-point fee per settled batch) would come with a production launch."
    />
  );
}
