import { PlaceholderPage } from "@/components/ui";

export default function Blog() {
  return (
    <PlaceholderPage
      eyebrow="Blog"
      title="Nothing published yet."
      body="Tiba shipped fast, in a single focused build sprint. Check back after the dust settles."
    />
  );
}
